// import request from '@/utils/request'
/**
 * 获取分类列表
 */
export const getCategory = () => {
  return Promise.resolve({ name: 'getCategory' })
}

export const init = () => {
  return Promise.resolve({ name: 'init' })
}
